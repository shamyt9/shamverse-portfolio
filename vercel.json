{
    "buildCommand": "npm install --prefix frontend/api && npm run build --prefix frontend/api && npm install --prefix frontend && npm run build --prefix frontend",
    "outputDirectory": "frontend/dist",
    "rewrites": [
        { "source": "/api/(.*)", "destination": "/api" },
        { "source": "/(.*)", "destination": "/index.html" }
    ]
}
