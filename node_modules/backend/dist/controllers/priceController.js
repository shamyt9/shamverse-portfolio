"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.deletePrice = exports.updatePrice = exports.createPrice = exports.getPrices = void 0;
const Price_1 = __importDefault(require("../models/Price"));
const getPrices = async (req, res) => {
    try {
        const prices = await Price_1.default.find({});
        res.json(prices);
    }
    catch (error) {
        res.status(500).json({ message: error.message });
    }
};
exports.getPrices = getPrices;
const createPrice = async (req, res) => {
    try {
        const price = new Price_1.default(req.body);
        const createdPrice = await price.save();
        res.status(201).json(createdPrice);
    }
    catch (error) {
        res.status(400).json({ message: error.message });
    }
};
exports.createPrice = createPrice;
const updatePrice = async (req, res) => {
    try {
        const price = await Price_1.default.findById(req.params.id);
        if (price) {
            Object.assign(price, req.body);
            const updatedPrice = await price.save();
            res.json(updatedPrice);
        }
        else {
            res.status(404).json({ message: 'Price not found' });
        }
    }
    catch (error) {
        res.status(400).json({ message: error.message });
    }
};
exports.updatePrice = updatePrice;
const deletePrice = async (req, res) => {
    try {
        const price = await Price_1.default.findById(req.params.id);
        if (price) {
            await price.deleteOne();
            res.json({ message: 'Price removed' });
        }
        else {
            res.status(404).json({ message: 'Price not found' });
        }
    }
    catch (error) {
        res.status(500).json({ message: error.message });
    }
};
exports.deletePrice = deletePrice;
